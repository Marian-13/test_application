<%= form_tag photos_path, method: :get do %>
  <%= hidden_field_tag :min_tag_id, photos.pagination.min_tag_id %>
  <%= hidden_field_tag :tag, params[:tag] %>
  <%= submit_tag 'Previous', name: nil, class: 'btn btn-default left' %>
<% end %>
